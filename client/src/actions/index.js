// aboutus
import {
  addCounter,
  increaseCounter,
  decreaseCounter,
  removeCounter,
  fetchData
} from "./counter";



const actions = {
  addCounter,
  increaseCounter,
  decreaseCounter,
  removeCounter,
  fetchData
};

export default actions;
